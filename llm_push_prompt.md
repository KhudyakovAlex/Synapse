# Веб-версия репозитория

Кроме заливки локальной версии репозитория на GitHub нужно сделать (или обновить) веб-версию репозитория.

## Правка markdown-файлов
Перед заливкой и конвертацией в HTML исправь орфографические и пунктуационные ошибки во всех markdown-файлах. 
Отформатируй их. Не надо ничего переформулировать и добавлять от себя.
Исправь нумерацию всех пунктов и подпунктов во всех документах. Порядок их в тексте менять не надо.

## Дизайн и стилистика

### Цветовая схема (IBM Carbon Design System)
- **Основной фон**: `#ffffff` (белый)
- **Вторичный фон**: `#f4f4f4` (светло-серый)
- **Фон кода**: `#e0e0e0`
- **Основной текст**: `#161616` (почти черный)
- **Вторичный текст**: `#525252` (серый)
- **Приглушенный текст**: `#8d8d8d`
- **Границы**: `#e0e0e0`
- **Акцентный цвет (primary)**: `#006a8e` (синий)
- **Акцентный цвет (secondary)**: `#004d6b` (темно-синий)
- **Ссылки**: `#006a8e`
- **Ссылки при наведении**: `#004d6b`

### Типографика
- **Основной шрифт**: IBM Plex Sans (Google Fonts)
- **Моноширинный шрифт**: IBM Plex Mono (для кода)

### Принципы дизайна
- Минималистичный, чистый интерфейс
- Четкие границы и разделители
- Скругление углов: 4px (минимальное, в стиле Carbon)
- Тени: легкие, ненавязчивые (`rgba(0, 0, 0, 0.1)`)
- Переходы: быстрые (0.2s), плавные
- Сетка: адаптивная, на основе CSS Grid

### Главная страница (INDEX/index.html)
- **Убрать**: шапку с навигацией
- **Убрать**: блок с описанием проекта (hero section)
- **Убрать**: блок со статистикой (1 PRD, 5 PDS, 11 таблиц, DALI)
- **Оставить**: только секции с документами (PRD и PDS)
- **Оставить**: футер с копирайтом и ссылкой на GitHub

## Требования

### Структура

**Вариант 1: Папка INDEX/ (рекомендуемый)**
- Создать папку `INDEX/` в корне репозитория
- Все файлы веб-версии лежат в `INDEX/`
- Структура папок в `INDEX/` повторяет структуру репозитория
- Главная страница: `INDEX/index.html`
- GitHub Pages публикует из папки `INDEX/`

Структура:
```
D:\Git\Synapse\
├── index.html              (текущая страница-навигатор)
├── PRD/                    (исходные MD-файлы)
├── PDS/                    (исходные MD-файлы)
└── INDEX/                  (веб-версия)
    ├── index.html          (главная веб-версии)
    ├── PRD/
    │   ├── SynapsePRD.html
    │   └── Идеи.html
    ├── PDS/
    │   ├── SynapsePDS_APP.html
    │   └── ...
    └── assets/             (стили, скрипты)
        ├── css/
        ├── js/
        └── img/
```

**Вариант 2: Корень репозитория (альтернатива)**
- Веб-версия находится прямо в корне
- `index.html` в корне — главная страница веб-версии
- HTML-файлы рядом с MD-файлами
- GitHub Pages публикует из корня `/`

**Вариант 3: Папка docs/**
- Создать папку `docs/` в корне
- GitHub Pages часто использует эту папку по умолчанию
- Аналогично варианту с `INDEX/`

### Скрипт конвертации
- **Расположение**: `INDEX/convert_to_html.py`
- **Запуск**: `cd INDEX && python convert_to_html.py`
- **Функция**: конвертирует все MD-файлы из `PRD/` и `PDS/` в HTML в `INDEX/PRD/` и `INDEX/PDS/`

### Конвертация документов
- Все Markdown-файлы (*.md) должны иметь HTML-вариант
- Сохранять форматирование: заголовки, списки, таблицы, блоки кода
- Конвертировать Mermaid диаграммы в интерактивный вид
- Для каждой Mermaid диаграммы создавать отдельную страницу (например, `db_schema_mermaid_diagram_0.html`)
- Сохранять относительные ссылки между документами

### Навигация
- Главная страница с навигацией по всем разделам (без шапки, без hero, без статистики)
- Хлебные крошки для навигации (только на страницах документов)
- Боковое меню с оглавлением документа
- Кнопки "Назад к списку" / "На главную"
- Ссылки на исходные MD-файлы в репозитории

### Интерактивность диаграмм Mermaid
- **Превью**: диаграмма отображается на странице документа
- **Клик**: открывает диаграмму на отдельной странице в полном размере
- **Зум**: колесо мыши увеличивает/уменьшает (0.5x - 5x)
- **Панорамирование**: перетаскивание мышью
- **Выделение текста**: Shift + выделение мышью (для копирования названий таблиц, полей и т.д.)
- **Точка зума**: левый верхний угол (диаграмма расширяется вправо и вниз)
- **Сброс выделения**: при новом клике с Shift старое выделение сбрасывается

### Оформление
- Единый стиль для всех страниц (IBM Carbon Design System)
- Адаптивный дизайн (mobile-friendly)
- Подсветка синтаксиса кода
- Иконки для разных типов документов
- Только светлая тема (темная тема удалена)

### GitHub Pages
- Настроить публикацию из папки `INDEX/` или через gh-pages branch
- URL: https://khudyakovalex.github.io/Synapse/

### Автоматизация
- Скрипт для автоматической конвертации MD → HTML
- При коммите в main/master — автоматическое обновление веб-версии

## Файлы для конвертации

### PRD (Техническое задание)
- `PRD/SynapsePRD.md` — основной документ с требованиями
- `PRD/Идеи.md` — дополнительные идеи

### PDS (Спецификация на разработку)
- `PDS/SynapsePDS_APP.md` — спецификация приложения
- `PDS/SynapsePDS_APP_UI.md` — UI приложения
- `PDS/SynapsePDS_DB.md` — база данных
- `PDS/SynapsePDS_FW.md` — прошивка
- `PDS/SynapsePDS_LLM.md` — интеграция с LLM
- `PDS/db_schema_mermaid.md` — схема БД (Mermaid)

### Корень
- `README.md` — описание проекта (если будет создан)

---

## ⚠️ ВАЖНО: Процедура обновления при коммитах

### При изменении MD-файлов (PRD/*.md или PDS/*.md):

**ОБЯЗАТЕЛЬНО выполнить следующие шаги:**

#### 1. Запустить конвертер
```bash
cd D:\Git\Synapse\INDEX
python convert_to_html.py
```

Скрипт автоматически:
- Найдёт все *.md файлы в папках PRD/ и PDS/
- Сконвертирует их в HTML с единым стилем
- Сохранит в INDEX/PRD/ и INDEX/PDS/
- Применит все правила форматирования:
  - Заголовки → `<h1>`, `<h2>`, `<h3>`, `<h4>`
  - Списки → `<ul>`, `<ol>`
  - Код → `<pre><code>`
  - Mermaid диаграммы → `<div class="mermaid">`
  - Два пробела в конце строки → `<br>`
  - Bold → `<strong>`, Italic → `<em>`

#### 2. Проверить изменения
```bash
git status
```

Должны появиться изменения в:
- `INDEX/PRD/*.html` (если менялись PRD/*.md)
- `INDEX/PDS/*.html` (если менялись PDS/*.md)

#### 3. Добавить все изменения
```bash
git add .
# или конкретные файлы
git add PRD/SynapsePRD.md INDEX/PRD/SynapsePRD.html
```

#### 4. Закоммитить
```bash
git commit -m "Update documentation: [описание изменений]"
```

#### 5. Отправить на GitHub
```bash
git push
```

#### 6. Проверить GitHub Pages
Через 1-2 минуты проверить: https://khudyakovalex.github.io/Synapse/

---

## 📁 Важные файлы

### `convert_to_html.py`
Автоматический конвертер MD → HTML
- **НЕ ИЗМЕНЯТЬ** без необходимости
- Поддерживает все текущие возможности
- При изменении MD-файлов — **ВСЕГДА запускать**

### `index.html` (корневой)
Файл-редирект на INDEX/index.html
- **НЕ ИЗМЕНЯТЬ** содержимое
- Автоматически перенаправляет на новую версию
- Работает через: `<meta http-equiv="refresh">` и JavaScript

### `INDEX/index.html`
Главная страница веб-версии
- Обновлять при добавлении новых документов
- Обновлять статистику (количество документов)
- Обновлять даты в разделе "Процесс разработки"

### `INDEX/assets/css/style.css`
Единый стиль для всех страниц
- Светлая/тёмная темы через CSS переменные
- **НЕ ИЗМЕНЯТЬ** без необходимости

### `INDEX/assets/js/main.js`
JavaScript для интерактивности
- Автогенерация оглавления
- Переключение тем
- Копирование кода
- **НЕ ИЗМЕНЯТЬ** без необходимости

---

## ✅ Чеклист перед коммитом

- [ ] Изменения в MD-файлах сохранены
- [ ] Запущен `python convert_to_html.py`
- [ ] Проверено, что HTML-файлы обновились
- [ ] Выполнен `git add` для всех изменений
- [ ] Написано осмысленное commit message
- [ ] Выполнен `git push`
- [ ] (Опционально) Через 2 минуты проверена веб-версия

---

## 🔄 Типичные сценарии

### Сценарий 1: Исправлена опечатка в PRD
```bash
# 1. Отредактировали PRD/SynapsePRD.md
# 2. Запустили конвертер
python convert_to_html.py
# 3. Закоммитили
git add PRD/SynapsePRD.md INDEX/PRD/SynapsePRD.html
git commit -m "Fix typo in SynapsePRD"
git push
```

### Сценарий 2: Добавлен новый раздел в PDS
```bash
# 1. Создали PDS/SynapsePDS_NEW.md
# 2. Запустили конвертер (создаст INDEX/PDS/SynapsePDS_NEW.html)
python convert_to_html.py
# 3. Обновили INDEX/index.html (добавили ссылку на новый документ)
# 4. Закоммитили всё
git add .
git commit -m "Add new PDS section: NEW"
git push
```

### Сценарий 3: Обновлена структура БД
```bash
# 1. Отредактировали PDS/SynapsePDS_DB.md и PDS/db_schema_mermaid.md
# 2. Запустили конвертер
python convert_to_html.py
# 3. Закоммитили
git add PDS/*.md INDEX/PDS/*.html
git commit -m "Update database schema"
git push
```

---

## 🚨 Частые ошибки

### ❌ Забыли запустить конвертер
**Проблема:** MD-файл изменён, но HTML-версия старая
**Решение:** Запустить `python convert_to_html.py` и сделать дополнительный коммит

### ❌ Изменили HTML напрямую
**Проблема:** При следующем запуске конвертера изменения потеряются
**Решение:** Всегда изменять только MD-файлы, HTML генерируется автоматически

### ❌ Не обновили INDEX/index.html при добавлении документа
**Проблема:** Новый документ есть, но на него нет ссылки на главной
**Решение:** Вручную добавить карточку с ссылкой в INDEX/index.html

---

## 📝 Формат commit message

**Хорошо:**
- `Update SynapsePRD: add section about sensors`
- `Fix line breaks in all documentation`
- `Add new PDS document: SynapsePDS_API`

**Плохо:**
- `update` (слишком общо)
- `fix` (что именно?)
- `changes` (какие?)